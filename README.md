$py=(Get-ChildItem .. -Filter python.exe -File -Recurse -ErrorAction SilentlyContinue | Where-Object {$_.FullName -like '*portfolio-agent\.venv\Scripts\python.exe'} | Select-Object -First 1).FullName; Write-Host "USING=$py"; & $py -m uvicorn server:app --host 127.0.0.1 --port 8000
