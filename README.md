Approved: diagnostic only. In market_event_scout.py, immediately before the existing Discovery response did not contain an events array raise, add minimal SAFE structural logging only: raw length, cleaned length, parsed top-level type, top-level keys, whether events exists, its type/count, and candidate object keys if fallback parsing selected one. DO NOT log raw response text, prompts, evidence, URLs, or sensitive content. Do not change parser behavior, prompt, model, 3-events rule, timeout, frontend, or any other bone. Then run ONE Trigger 1 theme once and report the diagnostic output only.
