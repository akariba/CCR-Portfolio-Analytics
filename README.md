Let the current US Trade Policy discovery test finish and report PASS/FAIL only. Then make NO source changes. Source the existing RUNTIME_ENV.ps1 in the same shell that will run the backend, verify RPR_THEME_GATE_MODEL and STEP2_SONNET_MODEL resolve to the already-approved claude-sonnet-5, restart server:app using the existing approved .venv, and run ONE AI Assist live test. Report: Trigger1 60k result, AI Assist PASS/FAIL, and whether proposed enhancements are returned. Do not modify any code, prompt, frontend, model routing, or working bone.
