IMPLEMENT NOW — RPR STEP 2.4 V6.0 STRICT PROMPT IMPLEMENTATION

BONE RULES ARE ABSOLUTE.

Do not redesign, refactor, simplify, reinterpret, or optimize the working application.
Do not touch Steps 1, 2.1, 2.2, or 2.3.
Do not change global CSS.
Do not change the v31 visual language.
Do not remove the existing V5.2 path; retain it only for rollback/parallel comparison.
Work only on the already-installed additive Step 2.4 V6 branch.

SOURCE OF TRUTH:
The new approved prompt is:
“Enhanced Prompt: Sector-Inherent Factor Identification, Metrics and Weighting — V6.0”

V6.0 MUST BE IMPLEMENTED AS WRITTEN.

==================================================
1. V6 INPUT CONTRACT
==================================================

The V6 model input is ONLY:

- Sector L1 Name
- Sector L2 Name
- Sector L3 Name
- optional Reference / As-of Date

Risk Factor Name and Risk Factor Details are NO LONGER INPUTS.

They must be independently generated by V6 through research.

Therefore:

- V6 generate-v6 MUST NOT require sector_inherent_factors.csv to contain the selected L3.
- V6 MUST NOT require predefined governed factor names/details.
- V6 MUST NOT call the legacy governed_sector() as a prerequisite for generation.
- V6 MUST NOT force model-generated factor names back to CSV names.
- V6 MUST NOT use the V5.2 _validate_and_govern() name/count enforcement.

IMPORTANT:
DO NOT DELETE OR MODIFY those V5.2 functions.
The V5.2 branch stays intact.
V6 must use its own additive validation path.

==================================================
2. RESEARCH-BASED FACTOR IDENTIFICATION
==================================================

For each L1/L2/L3 sector, V6 independently identifies exactly 4–5
sector-INHERENT credit risk factors.

Research must follow the V6 prompt.

Prioritize approved research capabilities/sources corresponding to:

- rating agency sector/industry outlook and methodology reports
- industry/trade association publications and benchmarking studies
- regulatory publications/disclosures
- aggregated sector-level financial data/disclosures
- reputable financial/industry sources covering structural sector trends

Use only the already-approved company retrieval/API/adapters.
NO public-web fallback.
NO mock data.
NO demo factor list.
NO fabricated source evidence.

Research recency:
prefer evidence from the last 12 months to establish current materiality.

Structural Persistence Test MUST be applied:

Would this factor plausibly have applied approximately 1 year ago and
still plausibly apply 1 year from now independently of a single dated event?

YES = sector inherent.
NO = exclude as event-driven.

Explicitly exclude one-off M&A, individual pending legislation,
single geopolitical shocks, current-quarter macro prints,
or anything whose materiality depends primarily on a discrete dated event.

Avoid overlapping/duplicate risk factors.

==================================================
3. REQUIRED SECTOR-LEVEL OUTPUT
==================================================

Return:

A. Sector Overview
2–3 sentences maximum:
- business model
- structural characteristics
- key credit drivers
- grounded in research

B. Risk Factor Identification
Exactly 4–5 factors.

For every factor:
- RF number
- Risk Factor Name
- Structural Rationale — 1 line maximum
- Source Basis — concise SOURCE-TYPE LABEL ONLY

Examples of source-basis format:
“Rating agency sector outlook”
“Industry association data”
“Regulatory disclosure”
“Company MD&A aggregation”

Do NOT put URLs/full citations in Source Basis.

==================================================
4. IMPORTANCE — STRICT V6 RULE
==================================================

Each risk factor MUST contain:

importance:
- HIGH
or
- MEDIUM

importance_score MUST BE DETERMINISTIC:

HIGH = 2
MEDIUM = 1

derived_weight_pct =
importance_score / sum(all factor importance_scores) * 100

Weights must sum to exactly 100% after deterministic rounding/remainder handling.

The model may determine HIGH vs MEDIUM using V6 methodology,
but the numeric score and weight calculation are deterministic backend logic.

HIGH:
direct, fast-transmitting, high-magnitude FCF impact;
structural/regulatory;
affects a majority of the sector simultaneously.

MEDIUM:
meaningful but episodic/slower transmitting;
partly manageable at issuer level through operational/financial buffers.

==================================================
5. RESTORE THE MISSING V31 IMPORTANCE CONTROL
==================================================

THIS IS CURRENTLY MISSING IN V6 UI AND MUST BE RESTORED.

The generated V6 factor header currently correctly displays something like:

HIGH · 25% · Net: 2/5

and the green Importance Justification section is also present.

KEEP BOTH.

But immediately in the factor card, restore the original v31 control:

FACTOR IMPORTANCE

[ High ] [ Medium ]

Use the EXACT existing v31 markup/classes/visual behaviour.
Do not invent a new switch/dropdown.

The generated V6 importance must preselect the appropriate button.

Example:
backend returns importance = HIGH
=> High control is selected.

If analyst changes High -> Medium:
importance becomes MEDIUM
importance_score becomes 1
ALL Step 2.4 weights are deterministically recomputed
total remains exactly 100%.

If Medium -> High:
importance_score becomes 2
weights recompute identically.

The green “Importance / Importance Justification” text from V6 remains visible.
IT DOES NOT REPLACE the High/Medium control.

==================================================
6. PER-FACTOR CONTENT
==================================================

Header must represent:

[Risk Factor N: Name]
Importance: High/Medium
Derived Weight: XX%
Net Residual Score: X/5

Rationale:
maximum 2 sentences.
Explain credit-risk mechanism, transmission pathway,
and why structurally acute for sector.

Importance Justification:
maximum 1 sentence.

==================================================
7. SET 1 — VULNERABILITY METRICS
==================================================

Minimum 3 metrics per factor.

Each metric contains:

- Metric name
- Source
- Direction
- Formula
- Very High quantitative threshold
- High quantitative threshold
- Moderate quantitative threshold
- Low quantitative threshold

Use quantitative thresholds only in threshold cells.

Annotate inverse metrics when lower = worse.

VERY HIGH represents sustained multi-period/multi-year deterioration,
not a single-year cyclical deviation.

Each factor MUST have a Critical Threshold Note below Set 1.

Score 5 requires SIMULTANEOUS breach of ALL critical conditions,
normally approximately 3 conditions.

A single Very High metric breach alone may produce Score 4 at most.

Single-year deviations during active restructuring/transition
must not automatically trigger Very High.

==================================================
8. SET 2 — BUFFER / MITIGANT METRICS
==================================================

Minimum 3 buffer metrics.

They must directly correspond to Set 1 metrics in the same order where required
by the V6 prompt.

Each contains:

- Metric
- Source
- Direction
- Formula
- Strong threshold
- Moderate threshold
- Weak threshold
- Negligible threshold

Quantitative thresholds only.

Buffer strength / formal credit effect must follow V6:

STRONG:
near-full offset;
formal -2 point effect when 2+ buffer metrics meet Strong threshold.

MODERATE:
partial meaningful protection;
formal -1 point effect for mixed Strong/Moderate profile.

WEAK:
0 point credit.

NEGLIGIBLE:
0 point credit.

Preserve the existing working arithmetic implementation that actually
reduces residual risk by the required amount:
Strong reduces raw score by 2;
Moderate reduces raw score by 1;
Weak/Negligible reduce by 0.

Floor = 1.
Ceiling = 5.

Do not reinterpret this.

==================================================
9. FIVE-ROW SCORING LOGIC
==================================================

Every factor must render exactly five scoring rows:

5
4
3
2
1

with:

- Vulnerability Profile
- Buffer Profile

Score 5:
worst-case Set 1 / ALL critical conditions simultaneously met;
negligible buffers.

Score 4:
High / Very High vulnerability;
Weak buffers.

Score 3:
High / Moderate vulnerability;
Moderate / Weak buffers.

Score 2:
Moderate / Low vulnerability;
Strong buffers.

Score 1:
Low vulnerability across metrics;
all buffers Strong.

Keep the V6 net-score formula note below the table.

==================================================
10. KEY PRINCIPLE + CREDIT IMPLICATION
==================================================

Every factor must include:

Key Principle:
maximum 2 sentences.
Explain most critical buffer(s), why they protect debt-service capacity,
and which buffers receive formal credit.

Credit Implication:
maximum 2 sentences.
Explain:
1. why this factor is critical and how it transmits to credit impairment
2. rationale for High vs Medium classification relative to other factors

==================================================
11. CROSS-FACTOR AGGREGATION
==================================================

Use deterministic importance scores:

HIGH = 2
MEDIUM = 1

Weight =
factor importance score / total importance scores.

Total weight = exactly 100%.

Composite Residual Score =
SUM(Derived Weight × Net Residual Risk Score)

Render the composite reference score.

Do not allow the model to calculate authoritative weights.
Backend calculation wins.

==================================================
12. STRICT FORMATTING
==================================================

Follow V6 prompt:

- no prose repetition of thresholds already in tables
- concise metric representation
- quantitative thresholds only in table threshold cells
- Risk Factor Identification rationale/source basis = max 1 line
- factor Rationale = maximum 2 sentences
- Importance Justification = maximum 1 sentence
- Key Principle = maximum 2 sentences
- Credit Implication = maximum 2 sentences
- scoring table always exactly 5 rows
- always show Net Score formula note
- always show Critical Threshold Note below Set 1
- Source Basis contains concise source-type label, not full URL

==================================================
13. V31 VISUAL BONE
==================================================

The current generated V6 screen is already close to correct visually.

DO NOT redesign it.

Preserve:
- .rf-card
- .rf-card-hdr
- .rf-card-body
- vulnerability table colours
- buffer table colours
- critical-threshold styling
- key-principle green styling
- scoring table
- credit-implication styling
- action-row
- feedback panel
- tabs
- navigation
- workflow status
- existing Step 2.4 buttons

Only restore the missing original v31 High/Medium Factor Importance control
and bind it to the V6 factor state.

Use the original:
icm-pm-rapid-portfolio-review-v31.html

as the exact markup/style reference for that control.

==================================================
14. FEEDBACK / REVISION V6
==================================================

revise-v6 must revise the V6-generated framework.

It must NOT revert to V5.2 governed/predefined taxonomy behaviour.

Analyst feedback may revise factor identification/framework,
but V6 validation still must enforce:

- 4–5 factors
- sector-inherent structural persistence
- no event-driven factors
- unique/non-overlapping factors
- required metrics
- High/Medium only
- deterministic importance scores/weights
- scoring rows 1–5
- critical threshold rule
- buffer-credit logic

==================================================
15. FINALIZATION
==================================================

finalize-v6 must validate and persist the CURRENT V6 framework,
including any analyst High/Medium change.

Before finalization verify:

factor count = 4 or 5
all factor names nonempty
all structural rationales nonempty
all source-basis labels nonempty
importance is HIGH/MEDIUM only
importance_score matches 2/1 exactly
weights total 100%
minimum 3 vulnerability metrics/factor
minimum 3 buffer metrics/factor
all required threshold bands exist
five scoring rows exactly
Net Score within 1–5
Composite Score mathematically correct

==================================================
16. DO NOT TOUCH
==================================================

Do NOT alter:

Trigger 1
Trigger 2
Step 2.1
Step 2.2
Step 2.3
AI Assist
feedback architecture outside Step2.4 V6
market_event_scout.py
Step1 prompts
Step2.1 prompts
Step2.3 prompts
global CSS
v31 page structure
existing V5.2 routes/service/data

No model downgrade.
No mocks.
No demo results.
No public internet fallback.
No hardcoded risk factors.

==================================================
17. ACCEPTANCE TEST
==================================================

Perform ONE controlled V6 test using one confirmed L3 sector.

PASS only if:

1. POST generate-v6 returns 200.
2. Research/model independently creates 4–5 factors without CSV factor names.
3. Sector Overview appears.
4. Risk-factor source basis appears.
5. Factor header shows HIGH/MEDIUM + weight + Net score.
6. The original v31 High/Medium toggle is visible.
7. Importance Justification remains visible separately.
8. Changing one High -> Medium updates:
   importance=MEDIUM
   importance_score=1
   all weights recomputed
   total=100%.
9. Every factor contains >=3 vulnerability metrics.
10. Every factor contains >=3 buffer metrics.
11. Every factor contains the Critical Threshold Note.
12. Every factor has exactly five scoring rows.
13. Key Principle appears.
14. Credit Implication appears.
15. Composite reference score is mathematically correct.
16. finalize-v6 returns 200.
17. V5.2 route still exists and was not modified.
18. Steps 1–2.3 remain byte/functionally untouched.

At completion report ONLY:

CHANGED FILES
WHY EACH FILE CHANGED
V6 GENERATE TEST RESULT
HIGH/MEDIUM UI TEST RESULT
WEIGHT = 100% TEST
FINALIZE-V6 RESULT
V5.2 PRESERVATION RESULT
STEPS 1–2.3 PRESERVATION RESULT

Do not perform unrelated cleanup or refactoring.
