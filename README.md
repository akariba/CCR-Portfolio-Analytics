Keep all working bone unchanged. Global Monetary Policy now proves the 60k fix works and returns 3 events. Diagnose ONLY the failed US Trade Policy & Tariffs discovery: output_chars≈44106, so it was not truncated, but parser says no events array. Run one call and report only raw length, truncation yes/no, top-level JSON type/keys, candidate/fallback keys selected, and why events is absent. Do not log content, do not modify parser/prompt/model/code. Also identify where the existing approved Sonnet 5 identifier is already configured for RPR_THEME_GATE_MODEL; do not invent one. Max 8 lines, diagnosis only.


Diagnose the AI Assist failure only. UI reports RPR_THEME_GATE_MODEL must be set to the exact organization-approved Sonnet 5 identifier, so proposed theme enhancements are not returned. Do NOT modify AI Assist/theme_assistant/theme_assistant_batch prompts, logic, frontend, or working bone. Find the exact Sonnet 5 identifier already used successfully in this approved project/runtime/company config and verify whether RPR_THEME_GATE_MODEL receives it in the actual backend process. If missing, propose only the smallest runtime/env fix. Do not invent a model identifier. Then run ONE minimal AI Assist request and report PASS/FAIL + model identifier source + whether suggestions were returned. Max 8 lines.
