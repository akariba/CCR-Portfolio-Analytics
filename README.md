Approved — proceed with Step 2.4 only.

Create ONLY the missing additive file:
backend/data/step24/sector_inherent_factors.csv

Use the exact live taxonomy:
L1 = Technology Media Telecom
L2 = Technology
L3 = Software

Transcribe the governed sector risk factors verbatim from:
Prompt/Use Case 1 - RPR/V2-Bottom-Up Approach/Demo/Step 2b Input - Risk Factor Taxonomy - Software.docx

Use exactly the schema required by the existing step24_sector_factors_service.py. Do not invent, summarize, rename, improve, or add factors.

Do NOT change Step2.2, Step2.3, Step2.4 service/routes/prompts, scoring, models, frontend, CSS, or any working bone.

Then run ONE live test using a Step2.2-confirmable Software portfolio:
catalog → Step2.2 select/finalize → Step2.4 generate → verify factors rendered → Step2.4 finalize.

Verify:

factors come from the approved Software taxonomy;
metrics/bands are generated by the existing Step2.4 LLM logic, not stored in CSV;
importance mapping remains HIGH=2, MEDIUM=1;
deterministic weights sum to exactly 100%;
finalization succeeds.

Report only PASS/FAIL, number of factors generated, weights sum, and files changed. If anything else must change, STOP and ask me first.
