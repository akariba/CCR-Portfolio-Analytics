Do not access or request access to my RPR repository. Stop the source-code inspection here. Your role is only to establish the official Citi-supported authentication architecture from the documentation you can access.

Based only on official Citi documentation already available to you, give me a final concise deployment specification for running R2D2 from a shared UNIX MarketDev server using M2M.

Confirm:

Exact supported authentication mode name for shared/server deployment.
Exact required credential concepts and whether they are COIN Client ID + Client Secret.
Whether H2M/Helix browser OAuth, localhost:8822 and Tectia tunnelling are unnecessary under M2M.
Whether a dedicated application/service COIN identity is expected rather than a personal SOEID credential.
Token endpoint/authentication flow used by M2M.
Certificate/TLS requirements.
How credentials should be supplied on MarketDev (environment variables, Vault, or documented alternative).
Whether M2M is appropriate for a FastAPI service shared by multiple users.
Any Citi approval/use-case registration required before obtaining the M2M Client ID and Client Secret.
Cite the exact Citi documentation source for every conclusion.

Do NOT inspect application code, do NOT propose code changes, do NOT display credentials/secrets, and clearly mark anything that is not explicitly documented as UNKNOWN.
